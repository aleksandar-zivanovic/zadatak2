<div class="flashes">
    {% for deletedUserMessage in app.flashes('deletedUser') %}
        <div class="bg-[lightgreen]">
            {{ deletedUserMessage }}
        </div>
    {% endfor %}

    {% for editedUserMessage in app.flashes('userEdited') %}
        <div class="bg-[lightgreen]">
            {{ editedUserMessage }}
        </div>
    {% endfor %}
</div>